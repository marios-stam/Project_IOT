class Config(object):
    # The others config...
    # The others config...

    # Flask-apscheduler

    # SCHEDULER_JOBSTORES = {
    #     'default': SQLAlchemyJobStore(url='sqlite:///flask_context.db')
    # }
    SCHEDULER_API_ENABLED = True
